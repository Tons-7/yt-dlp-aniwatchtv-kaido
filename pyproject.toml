[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.distutils.bdist_wheel]
universal = true

[project]
name = "yt-dlp-aniwatchtv-kaido"
version = "3.1.0"
dependencies = [
    "requests>=2.32.4"
]
requires-python = ">=3.11"
