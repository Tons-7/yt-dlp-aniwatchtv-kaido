[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.distutils.bdist_wheel]
universal = true

[project]
name = "yt-dlp-aniwatchtv"
version = "3.0.1"
dependencies = [
    "requests>=2.32.4"
]
requires-python = ">=3.11"
